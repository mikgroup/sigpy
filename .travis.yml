language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"

install:
  - pip install -r requirements.txt
  - pip install codecov pytest-cov flake8

script:
  - pytest --cov=./
  - flake8

after_success:
  - codecov
